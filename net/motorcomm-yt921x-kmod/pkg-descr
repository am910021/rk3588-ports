Native FreeBSD etherswitch(4) kernel module for Motorcomm YT921x Ethernet
switches.  It identifies YT9215S devices, configures their fixed RGMII CPU
links, and exposes switch ports through etherswitchcfg(8).
